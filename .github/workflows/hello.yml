name: Hello World Test

on:
  workflow_dispatch: # Permet de lancer à la main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Say Hello
        run: echo "✅ KosMath AI Build system is working!"
